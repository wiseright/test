lista = [1, 2, 3]

def show_max(lista:list) -> float:
    print(f"Massimo in lista: {max(lista)}")

def show_min(lista: list) -> float:
    print(f"Minimo in lista: {min(lista)}")

def show_len(lista: list) -> float:
    print(f"Lunghezza lista: {len(lista)}")

show_max(lista)
show_min(lista)
show_len(lista)

